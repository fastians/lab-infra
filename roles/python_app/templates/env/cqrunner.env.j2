# CadQuery runner — executes generated CadQuery code and returns STEP (for /cad-gen2).
# Reached only by the backend over the internal network (never public). Runs untrusted,
# model-generated Python, so it carries NO secrets and no backend credentials.

CQ_RUNNER_PORT=8003
CQ_EXEC_TIMEOUT_S=20
CQ_MAX_CODE_CHARS=40000
CQ_MAX_WORKERS=2
