GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY={{ vault_google_api_key | default('PASTE_YOUR_ACTUAL_API_KEY_HERE') }}
OPENROUTER_API_KEY={{ vault_openrouter_api_key | default('PASTE_YOUR_ACTUAL_API_KEY_HERE') }}
OPENROUTER_MODEL={{ vault_openrouter_model | default('openai/gpt-4o-mini') }}
OPENROUTER_HTTP_REFERER=https://{{ primary_domain }}
OPENROUTER_APP_TITLE="{{ vault_openrouter_app_title | default('Mek Lab') }}"
FRONT_SERVER_URL=https://{{ primary_domain }}
# Comma-separated browser origins for CORS (FastAPI). SPA is served at mek-lab.com (CloudFront), not Vercel.
CORS_ORIGINS=https://{{ primary_domain }},https://www.{{ primary_domain }},https://mek-lab.vercel.app,http://localhost:3000,http://localhost:5173
LLM_SERVER_URL={{ llm_server_internal_url | default('http://127.0.0.1:8002') }}
SALOME_SERVER_URL=https://{{ salome_public_host }}
GEO_SERVER_URL={{ geo_server_internal_url | default('http://127.0.0.1:8001') }}
DATABASE_URL={{ vault_backend_database_url }}
SECRET_KEY={{ vault_backend_secret_key }}
LLM_API_SECRET_KEY={{ vault_internal_api_key }}
SENDGRID_API_KEY={{ vault_sendgrid_api_key }}
SENDGRID_FROM_EMAIL={{ vault_sendgrid_from_email }}
AWS_ACCESS_KEY_ID={{ vault_aws_access_key_id }}
AWS_SECRET_ACCESS_KEY={{ vault_aws_secret_access_key }}
AWS_SES_REGION={{ vault_aws_ses_region | default('ap-northeast-2') }}
AWS_SES_FROM_EMAIL={{ vault_aws_ses_from_email | default('contact@mek-lab.com') }}
AWS_SES_SENDER_NAME="{{ vault_aws_ses_sender_name | default('Mek-Lab') }}"
# Legacy Postmark (quota exhausted)
POSTMARK_API_KEY={{ vault_postmark_api_key }}
POSTMARK_SENDER=Meklab
POSTMARK_SERVER_ID=17879776
POSTMARK_FROM_EMAIL={{ contact_email }}
POSTMARK_SIGNUP_TEMPLATE_ID=43149087
POSTMARK_FORGET_PW_TEMPLATE_ID=43148701
NOTIFY_EMAIL={{ vault_notify_email | default('contact@mek-lab.com') }}
GMAIL_USER={{ vault_gmail_user | default('') }}
GMAIL_APP_PASSWORD={{ vault_gmail_app_password | default('') }}
# Toss Payments — server only (subscription / billing key). Pair with VITE_TOSS_CLIENT_KEY on frontend.
TOSS_SECRET_KEY={{ vault_toss_secret_key | default('') }}

# CadQuery runner (executes generated CadQuery → STEP for /cad-gen2). Runs on the GEO host
# at :8003, reached over the internal network. Empty disables the render preview gracefully.
CQ_RUNNER_URL={{ cq_runner_internal_url | default('') }}
