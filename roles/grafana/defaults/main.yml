---
# When Grafana runs on the same host as Prometheus/Loki (e.g. monitoring server), use localhost.
# Override in group_vars when Grafana runs elsewhere.
prometheus_server: "localhost"
loki_server: "localhost"
